package com.g3c1.aide.feature_account.presentation.utils

object Types {
    enum class TokenType {
        ACCESS,
        REFRESH,
        EXPIRED
    }

    enum class AccoutDataType {
        ID,
        PASSWORD
    }
}